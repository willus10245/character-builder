{
  "repository": {},
  "license": "MIT",
  "scripts": {
    "deploy": "cp -R static/* ../priv/static && parcel build ./js/app.js ./css/app.scss --out-dir ../priv/static --no-cache",
    "watch": "cp -R static/* ../priv/static && parcel watch ./js/app.js ./css/app.scss --out-dir ../priv/static"
  },
  "dependencies": {
    "phoenix": "file:../deps/phoenix",
    "phoenix_html": "file:../deps/phoenix_html"
  },
  "devDependencies": {
    "parcel": "latest",
    "sass": "^1.26.5"
  }
}
